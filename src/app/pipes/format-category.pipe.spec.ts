import { FormatCategoryPipe } from './format-category.pipe';

describe('FormatCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
